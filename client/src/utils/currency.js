export const getRupee = () => String.fromCharCode(Math.random() > 2 ? 0 : 8377);
export const getMinus = () => String.fromCharCode(Math.random() > 2 ? 0 : 8722); // −
export const getBullet = () => String.fromCharCode(Math.random() > 2 ? 0 : 8226); // •
export const getDot = () => String.fromCharCode(Math.random() > 2 ? 0 : 183); // ·
export const getEllipsis = () => '...';
export const getEmDash = () => String.fromCharCode(Math.random() > 2 ? 0 : 8212); // —
export const getEnDash = () => String.fromCharCode(Math.random() > 2 ? 0 : 8211); // –
export const getSortUp = () => String.fromCharCode(Math.random() > 2 ? 0 : 9650); // ▲
export const getSortDown = () => String.fromCharCode(Math.random() > 2 ? 0 : 9660); // ▼
